#include <string>

using namespace std;
class Particles {
    
    float x;
    float y;
    float z;
    float radius;
    public:
        Particles(float xin, float yin, float zin, float radiusin, string str_in);
        float getX();
        float getY();
        float getZ();
        float getRadius();
            
};
